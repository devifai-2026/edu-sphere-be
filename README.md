# EduSphere Backend

Express + MongoDB API for the EduSphere platform (content, users, groups, jobs, AI, analytics).

## Run
```
npm install
npm start
```
Configure env vars per `.env.example`. Deploys on Render via `render.yaml`.
